MNIST 数据集是图像分类中广泛使用的数据集之一，但作为基准数据集过于简单。我们将使用类似但更复杂的 Fashion-MNIST 数据集。这是一个手写数据集。
读取一小批量数据，大小为 batch_size。
将展平每个图像，将它们视为长度为 784 的向量。因为我们的数据集有 10 个类别，所以网络输出维度为 10。
给定一个矩阵 x，我们可以对所有元素求和。这边的输出当中，维度为 0，则输出为行向量，1 就是列向量。
实现交叉熵损失函数。
Accumulator 实例中创建了 2 个变量，用于分别存储正确预测的数量和预测的总数量。
进行迭代十个周期。
感知机：感知机定义：给定输入 x，权重 w，和偏移 b，感知机输出（公式未完整显示，模型如上标注）。
训练感知机：初始化 w=0 和 b=0；重复执行：若 y・(w・xi + b) ≤ 0，则 w←w + yi・xi，b←b + yi；直到所有样本分类正确。这等价于使用批量大小为 1 的梯度下降，并使用如下的损失函数 e (y, x, w) = max (0, -y (w・x))。
收敛定理：数据在半径 r 内；余量分类两类：y (x・w + b) > ρ；约束：||w||² + b² ≤1；结论：感知机保证在（未明确完整步数，文档显示为 ρ² 相关步骤）步后收敛。
感知器的权重为 0。
感知机缺陷：XOR 问题（Minsky & Paper,1969）：感知机不能拟合 XOR 函数，它只能产生线性分割面；XOR 学习相关表格（内容：1、2、3、4 列，含 +、- 符号及 product 列，具体对应输入输出关系）。
网络结构：输入层（x1、x2、x3 等）；隐藏层（h1、h2、h3、h4、h5 等，标注 “隐藏层大小是超参数”）；输出层（o1、o2、o3 等）；关键说明：唯一可以做到（拟合非线性函数）的是设置隐藏层大小；但注意，一定要有激活函数，否则没有意义。
激活函数：Tanh 激活函数：将输入投影到 (-1,1)，公式：tanh (x) = (1 - exp (-2x)) / (1 + exp (-2x))；其他提及：常用激活函数是 Sigmoid、Tanh、ReLU；有 “较为平和的版本” 相关标注（对应未明确函数图像）。
多隐藏层相关：层间关系为 h1=σ(W1x + b1)，h2=σ(W2h1 + b2)，h3=σ(W3h2 + b3)，o=W4h3 + b4（σ 为激活函数）；超参数包括隐藏层数、每层隐藏层的大小。
核心总结：多层感知机使用隐藏层和激活函数来得到非线性模型；使用 Softmax 来处理多类分类；超参数为隐藏层数和各个隐藏层大小。
唯一可以做到就是隐藏层有多大，但注意，一定要有激活函数，否则没有意义